# --- LLM (provider-agnostic via LiteLLM; anthropic kept for its native web-search tool) ---
litellm>=1.55.0
anthropic>=0.69.0
# Pin tokenizers to a version with prebuilt wheels (avoids a Rust source build on
# restricted networks). If pip still tries to build, install with: pip install --only-binary=:all: -r requirements.txt
tokenizers==0.23.1

# --- Agent orchestration ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- RAG / embeddings (local, no API key needed) ---
fastembed>=0.4.0
numpy>=1.26.0

# --- TLS via OS trust store (works behind corporate TLS-inspecting proxies) ---
truststore>=0.9.0

# --- Database: PostgreSQL + pgvector ---
psycopg[binary]>=3.2.0
pgvector>=0.3.0
SQLAlchemy>=2.0.0

# --- PDF / document parsing ---
pymupdf>=1.24.0

# --- Backend API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- UI ---
streamlit>=1.39.0

# --- Utilities ---
python-dotenv>=1.0.0
pydantic>=2.8.0
requests>=2.32.0
